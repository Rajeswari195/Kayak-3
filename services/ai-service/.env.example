# BEGIN WRITING FILE CODE
# -----------------------------------------------------------------------------
# services/ai-service/.env.example
# -----------------------------------------------------------------------------
# Example environment variables for the FastAPI-based AI Recommendation Service.
#
# Usage:
#   1. Copy this file to `.env.local` (or `.env`) in the same directory:
#        cp .env.example .env.local
#   2. Replace placeholder values with real ones for your environment.
#
# Notes:
#   - The Pydantic BaseSettings loader is defined in `app/config/settings.py`.
#   - All variables here are read by that loader, which also supports override
#     via standard environment variables.
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# Environment & server configuration
# -----------------------------------------------------------------------------
ENV=development

# Port on which the AI service will listen for HTTP and WebSocket requests.
# This should match the URLs used by the client (VITE_AI_API_BASE_URL,
# VITE_AI_WS_URL).
AI_SERVICE_PORT=8001

# Logging verbosity (e.g., debug, info, warn, error).
LOG_LEVEL=info


# -----------------------------------------------------------------------------
# SQLite / SQLModel configuration
# -----------------------------------------------------------------------------
# SQLite connection string for deals, routes, tagged offers, and watch rules.
# For local development, a file-based SQLite DB is sufficient.
#
# Example:
#   DEALS_SQLITE_URL=sqlite:///./data/deals.sqlite3
# -----------------------------------------------------------------------------
DEALS_SQLITE_URL=sqlite:///./data/deals.sqlite3


# -----------------------------------------------------------------------------
# Kafka configuration
# -----------------------------------------------------------------------------
# Comma-separated list of Kafka broker addresses used by the AI service to:
#   - Consume raw supplier feeds and deals topics.
#   - Emit deal events and notifications.
#
# Example:
#   KAFKA_BROKERS=localhost:9092
# -----------------------------------------------------------------------------
KAFKA_BROKERS=localhost:9092


# -----------------------------------------------------------------------------
# Optional: Core API reference & feature toggles
# -----------------------------------------------------------------------------
# Base URL for the core-api service (if the AI service needs to query it).
CORE_API_BASE_URL=http://localhost:4000

# Maximum number of recommended bundles to return from /bundles.
MAX_BUNDLES_PER_REQUEST=10

# Maximum number of watches a single user can register.
MAX_WATCHES_PER_USER=50
# -----------------------------------------------------------------------------
# END OF FILE
