# BEGIN WRITING FILE CODE
# -----------------------------------------------------------------------------
# client/.env.example
# -----------------------------------------------------------------------------
# Example environment variables for the React SPA (Vite-based) client.
#
# Usage:
#   1. Copy this file to `.env.local` in the same directory:
#        cp .env.example .env.local
#   2. Adjust the values to match your local or deployed environment.
#
# Notes:
#   - Vite only exposes variables prefixed with `VITE_` to the browser.
#   - Do NOT commit any real secrets or production URLs; this file is
#     intentionally generic and safe to share.
# -----------------------------------------------------------------------------


# -----------------------------------------------------------------------------
# Core API base URL
# -----------------------------------------------------------------------------
# The base URL for the Node.js + Express core API.
# In local development, we typically run core-api on port 4000.
#
# Example:
#   VITE_API_BASE_URL=http://localhost:4000
# -----------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:4000


# -----------------------------------------------------------------------------
# AI Service base URL (HTTP)
# -----------------------------------------------------------------------------
# The base URL for the FastAPI-based AI Recommendation Service.
# In local development, we typically run ai-service on port 8001.
#
# Example:
#   VITE_AI_API_BASE_URL=http://localhost:8001
# -----------------------------------------------------------------------------
VITE_AI_API_BASE_URL=http://localhost:8001


# -----------------------------------------------------------------------------
# AI Service WebSocket URL
# -----------------------------------------------------------------------------
# WebSocket endpoint for watched-deal events.
# This should point to the `/events` WS endpoint exposed by ai-service.
#
# For local development:
#   - Use `ws://` when not using HTTPS/SSL.
#   - Use `wss://` in production.
#
# Example:
#   VITE_AI_WS_URL=ws://localhost:8001/events
# -----------------------------------------------------------------------------
VITE_AI_WS_URL=ws://localhost:8001/events


# -----------------------------------------------------------------------------
# Optional: Feature flags or analytics keys (client-safe only)
# -----------------------------------------------------------------------------
# Add any additional public configuration here as needed, e.g.:
#
# VITE_ENABLE_MOCK_DATA=true
# VITE_ANALYTICS_ENABLED=false
#
# Remember: never put secrets (DB passwords, JWT secrets, API keys that
# shouldn't be exposed) in client env files.
# -----------------------------------------------------------------------------
# END OF FILE
